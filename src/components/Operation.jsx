import  {useState} from 'react'
import  '../App.css'

export  const Operation = (props) => {

    



    return(
      <button className = {props.class}>
      {props.operation}
      </button>

    
    )
}